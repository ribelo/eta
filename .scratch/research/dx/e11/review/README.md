# DX-E11 W6 review packet

## One-call comparison

- E19 reference: `.scratch/research/dx/e19/review/w6-new.ml`
- E11 candidate: `w6-run.ml`

The E19 fixture already improved runtime-wide fake-clock wiring, but its test
still creates a clock, forks `Runtime.run`, waits for each sleeper, advances
10/20/40 manually, and awaits the result. E11 replaces that protocol with one
`Run.run` call and assertions over returned data.

Nonblank, non-comment code lines: **25 E19 vs 22 E11** (12% reduction). Line
count understates the change: E11 removes the switch/runtime parameters and the
entire scheduler-driving loop from the test's responsibilities. The exact sleep
history is data on `outcome`, not inferred from final clock time.

## Broken-output packet

`broken-output.txt` is generated by
`.scratch/research/dx/e11/redteam/run.sh`. The deliberately broken linear retry
sleeps 10/20/30 while the expected golden record says 10/20/40.

Rubric:

- **What:** expected and received records differ at sleep entry `[2]`, 40ms vs
  30ms.
- **Where:** assertion names `Run.outcome.sleeps` and the retry-backoff case.
- **What next:** assertion says to inspect the schedule constructor.

Rating: **3/3 — actionable without rerunning under a debugger.**
