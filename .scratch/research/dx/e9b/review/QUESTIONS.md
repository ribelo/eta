# DX-E9b Review Questions

Answer from the packet sources only (`transfer.ml`, `loads.ml`, and any
`syntax.mli` / `Effect.par` docs you are given). Do not use branch journals.

1. How many fibers fork in `transfer.ml` for the product of debit and credit?
2. If debit fails in `transfer.ml`, does credit run?
3. Is the effect order guaranteed in `transfer.ml`?
4. What does `Effect.par` do in `loads.ml`?
5. Where would you look for the concurrency in `loads.ml`?
