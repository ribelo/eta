# Review packet manifest

- `w1-old.ml` — synthetic W1 program against old API (`recover`).
- `w1-new.ml` — same W1 program against new API (`fold`).
- `site2-old.ml` — old spelling of `examples/quickstart.ml` pure recovery.
- `site2-new.ml` — current `examples/quickstart.ml` pure recovery via `fold`.
- `site3-old.ml` — old spelling of stream `Tap_error` recovery in `lib/stream/eta_stream.ml`.
- `site3-new.ml` — current stream `Tap_error` recovery via `bind_error`.
- `QUESTIONS.md` — teach-back prompts for blind review.
- `MANIFEST.md` — this file.
