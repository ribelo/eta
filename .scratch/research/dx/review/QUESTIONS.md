# Teach-back questions (blinded)

1. Does `sync_result` catch exceptions into the typed failure channel?
2. What does `discard` do to typed failures?
3. What does `ignore_errors` do to defects?
4. In `race_either left right`, which argument is `` `Left ``?
