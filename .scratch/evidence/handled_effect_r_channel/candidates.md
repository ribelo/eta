# Candidate Ledger

| Candidate | Why it is plausible | Evidence needed to win | Evidence that would falsify it | Current evidence | Status |
| --- | --- | --- | --- | --- | --- |
| R-B object-row env | Already passed V-R10: row inference carries transitive requirements and boot-site missing services fail by method name. | Remains baseline. | A new approach preserves the same zero-service argument shape with better safety/locality. | Old `scratch/r_research/r_b_env_row.ml`. | Baseline |
| H-C combined `handled_effect` service effect | One local handler argument can perform all service operations; missing raw handler cannot happen because `perform` needs a handler value. | Normal service story works; provider absence becomes static or startup-checked per service. | Collapsing Db/Log into one effect means omitted operation handling is runtime behavior, not static provider evidence. | `Handled_combined` runtime smoke; `run_db_only` runtime missing-log check. | Active |
| H-S separate `handled_effect` effect per service | Strongest Jane Street shape: each service has its own typed local handler; continuations statically require forwarded handlers. | Normal service story works; missing forwarded handlers fail at compile time; `a` does not expose services or handler plumbing. | `a` must accept Db/Log handler arguments or cannot call leaves. | `Handled_separate` runtime smoke and negative fixtures. | Active |
| H-F root-installed service effects | Old additive service hypothesis: install once and arbitrary leaves perform. | Handler survives or is recoverable at all Eta/Eio fiber boundaries with one wrapper per fork site. | Same old locality issue, or `handled_effect` requires explicit handler capture that cannot cross Eio callbacks. | `neg_eio_fiber_capture.ml` tests capture. | Active |
