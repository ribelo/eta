# PPX OxCaml Probe

Backlog: Effet-OxCaml-3gk.

Question: can ppx_effet's current expansion style work with OxCaml
mode/kind-annotated Effect APIs, and does the pinned ppxlib carry mode syntax?

Run:

    nix develop .#oxcaml -c bash scratch/oxcaml_research/ppx_oxcaml_probe/run.sh

