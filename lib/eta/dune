(library
 (name eta)
 (public_name eta)
 (synopsis "Typed functional effects for OCaml: typed failures, exits, schedules, retry, race, timeout, and detached fibers")
(private_modules
  effect_erasure
  effect_concurrent
  effect_core
  effect_instrument
  effect_resource
  effect_schedule
  effect_supervisor_scope
  runtime_core
  runtime_erasure
  runtime_instrument
  runtime_capabilities
  runtime_observability
  runtime_trace_context
  runtime_supervisor))
