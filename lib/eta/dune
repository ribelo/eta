(library
 (name eta)
 (public_name eta)
 (synopsis "Typed functional effects for OCaml: typed failures, exits, schedules, retry, race, timeout, and detached fibers; backed by Eio")
 (private_modules
  blocking_runtime
  island_runtime
  runtime_core
  runtime_instrument
  runtime_observability
  runtime_supervisor
  runtime_supervisor_types)
 (libraries eio eio.unix cstruct portable eta_par threads))

(env
 (release
  (ocamlopt_flags
   (:standard -zero-alloc-check all))))
