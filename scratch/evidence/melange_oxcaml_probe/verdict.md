# Verdict

Generated by running:

```sh
bash scratch/evidence/melange_oxcaml_probe/run.sh
```

Current local result:

- `[@zero_alloc]` parses under stock OCaml.
- `[@zero_alloc]` is not enforced by stock OCaml: an allocating tuple function
  annotated with `[@zero_alloc]` compiles.
- Eta's OxCaml mode/kind syntax does not parse under stock OCaml:
  `@ portable`, `@ many`, `: immutable_data`,
  `: value mod portable`, `global_`, and `@ local` all fail.
- `melc` was not installed in this local switch, so the Melange-specific pass
  was skipped. Since Melange compiles OCaml syntax, not OxCaml syntax, these
  parser failures are already enough to show that Eta's current source cannot be
  sent through Melange unchanged.
