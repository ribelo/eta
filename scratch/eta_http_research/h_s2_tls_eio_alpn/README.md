# H-S2 tls-eio ALPN Probe

Question: can `tls-eio` plus `ocaml-tls` negotiate ALPN outcomes over local
Eio TCP flows under the OxCaml switch?

This lab is local fixture evidence only. Production smoke and TLS quality
evidence belong in H-S2/H-S3 result entries after this substrate compiles and
the local matrix is stable.

