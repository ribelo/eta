diff --git a/hpack/src/decoder.ml b/hpack/src/decoder.ml
index d91a0a2..a58337d 100644
--- a/hpack/src/decoder.ml
+++ b/hpack/src/decoder.ml
@@ -53,8 +53,16 @@ let set_capacity { max_capacity; table } capacity =
     Dynamic_table.set_capacity table capacity;
     Ok ())

-let decode_headers =
+let decode_headers_with_limit ?max_headers ?max_header_list_size =
   let decoding_error = return (Error Decoding_error) in
+  let account_header count bytes name value =
+    let count = count + 1 in
+    let bytes = bytes + String.length name + String.length value + 32 in
+    match max_headers, max_header_list_size with
+    | Some limit, _ when count > limit -> Error Decoding_error
+    | _, Some limit when bytes > limit -> Error Decoding_error
+    | _ -> Ok (count, bytes)
+  in
   (* From RFC7541§5.1:
    *   decode I from the next N bits. *)
   let decode_int prefix n =
@@ -138,7 +146,7 @@ let decode_headers =
         decode_string
   in
   fun ({ table; _ } as t) ->
-    let rec loop acc saw_first_header =
+    let rec loop acc count bytes saw_first_header =
       at_end_of_input >>= fun is_eof ->
       if is_eof
       then return (Ok acc)
@@ -153,7 +161,10 @@ let decode_headers =
           decode_int b 7 >>= fun index ->
           match get_indexed_field table index with
           | Ok (name, value) ->
-            loop ({ name; value; sensitive = false } :: acc) true
+            (match account_header count bytes name value with
+            | Ok (count, bytes) ->
+              loop ({ name; value; sensitive = false } :: acc) count bytes true
+            | Error _ as e -> return e)
           | Error _ as e -> return e
         else if b land 0b1100_0000 == 0b0100_0000
         then
@@ -164,12 +175,15 @@ let decode_headers =
            *   Section 5.1). *)
           decode_header_field table b 6 >>= function
           | Ok (name, value) ->
-            (* From RFC7541§6.2.1: Literal Header Field with Incremental Indexing
-             *   A literal header field with incremental indexing representation
-             *   results in appending a header field to the decoded header list
-             *   and inserting it as a new entry into the dynamic table. *)
-            Dynamic_table.add table (name, value);
-            loop ({ name; value; sensitive = false } :: acc) true
+            (match account_header count bytes name value with
+            | Ok (count, bytes) ->
+              (* From RFC7541§6.2.1: Literal Header Field with Incremental Indexing
+               *   A literal header field with incremental indexing representation
+               *   results in appending a header field to the decoded header list
+               *   and inserting it as a new entry into the dynamic table. *)
+              Dynamic_table.add table (name, value);
+              loop ({ name; value; sensitive = false } :: acc) count bytes true
+            | Error _ as e -> return e)
           | Error _ as e -> return e
         else if b land 0b1111_0000 == 0
         then
@@ -180,7 +194,10 @@ let decode_headers =
            *   5.1). *)
           decode_header_field table b 4 >>= function
           | Ok (name, value) ->
-            loop ({ name; value; sensitive = false } :: acc) true
+            (match account_header count bytes name value with
+            | Ok (count, bytes) ->
+              loop ({ name; value; sensitive = false } :: acc) count bytes true
+            | Error _ as e -> return e)
           | Error _ as e -> return e
         else if b land 0b1111_0000 == 0b0001_0000
         then
@@ -191,7 +208,10 @@ let decode_headers =
            *  header field without indexing (see Section 6.2.2). *)
           decode_header_field table b 4 >>= function
           | Ok (name, value) ->
-            loop ({ name; value; sensitive = true } :: acc) true
+            (match account_header count bytes name value with
+            | Ok (count, bytes) ->
+              loop ({ name; value; sensitive = true } :: acc) count bytes true
+            | Error _ as e -> return e)
           | Error _ as e -> return e
         else if b land 0b1110_0000 == 0b0010_0000
         then
@@ -210,8 +230,10 @@ let decode_headers =
           else
             decode_int b 5 >>= fun capacity ->
             match set_capacity t capacity with
-            | Ok () -> loop acc saw_first_header
+            | Ok () -> loop acc count bytes saw_first_header
             | Error _ as e -> return e
         else decoding_error
     in
-    loop [] false
+    loop [] 0 0 false
+
+let decode_headers decoder = decode_headers_with_limit decoder
diff --git a/hpack/src/hpack.mli b/hpack/src/hpack.mli
index 321094b..f0a5cf7 100644
--- a/hpack/src/hpack.mli
+++ b/hpack/src/hpack.mli
@@ -90,4 +90,13 @@ module Decoder : sig
   val decode_headers : t -> (header list, error) result Angstrom.t
   (** [decode_headers decoder] creates an Angstrom parser that will decode a
       header block and return a list of the decoded headers *)
+
+  val decode_headers_with_limit :
+    ?max_headers:int ->
+    ?max_header_list_size:int ->
+    t ->
+    (header list, error) result Angstrom.t
+  (** [decode_headers_with_limit decoder] decodes headers like
+      {!decode_headers}, but fails with [Decoding_error] as soon as the decoded
+      header count or HTTP/2 header-list size exceeds the supplied limits. *)
 end
diff --git a/lib/config.ml b/lib/config.ml
index 4814a70..d9b40e4 100644
--- a/lib/config.ml
+++ b/lib/config.ml
@@ -40,6 +40,8 @@ type t =
   ; enable_server_push : bool
   ; max_concurrent_streams : int32
   ; initial_window_size : int32
+  ; max_header_list_size : int option
+  ; max_header_count : int option
   }

 let default =
@@ -73,6 +75,8 @@ let default =
      * 128 MiB. *)
     (* TODO(anmonteiro): validate the default somewhere. *)
     initial_window_size = Int32.shift_left 1l 27
+  ; max_header_list_size = None
+  ; max_header_count = None
   }

 let to_settings
@@ -80,6 +84,7 @@ let to_settings
     ; max_concurrent_streams
     ; initial_window_size
     ; enable_server_push
+    ; max_header_list_size
     ; _
     }
   =
@@ -88,4 +93,5 @@ let to_settings
   ; max_concurrent_streams
   ; initial_window_size
   ; enable_push = enable_server_push
+  ; max_header_list_size
   }
diff --git a/lib/h2.mli b/lib/h2.mli
index 4130166..84a1eaf 100644
--- a/lib/h2.mli
+++ b/lib/h2.mli
@@ -675,6 +675,13 @@ module Config : sig
     ; initial_window_size : int32
     (** [initial_window_size] specifies the initial window size for flow control
         tokens. Defaults to [65535] *)
+    ; max_header_list_size : int option
+    (** [max_header_list_size] specifies the largest decoded HTTP/2 header-list
+        size the sender is willing to receive. When present, it is advertised
+        with SETTINGS_MAX_HEADER_LIST_SIZE and enforced during HPACK decoding. *)
+    ; max_header_count : int option
+    (** [max_header_count] specifies the largest decoded header field count the
+        sender is willing to receive. It is enforced during HPACK decoding. *)
     }

   val default : t
diff --git a/lib/server_connection.ml b/lib/server_connection.ml
index 4ac7d7a..8b21d73 100644
--- a/lib/server_connection.ml
+++ b/lib/server_connection.ml
@@ -406,6 +406,12 @@ let handle_headers_block t stream active_stream partial_headers flags bytes =

 let handle_trailer_headers = handle_headers_block ~is_trailers:true

+let decode_headers t =
+  Hpack.Decoder.decode_headers_with_limit
+    ?max_headers:t.config.max_header_count
+    ?max_header_list_size:t.config.max_header_list_size
+    t.hpack_decoder
+
 let open_stream t ~priority stream_id =
   if not Stream_identifier.(stream_id > t.max_client_stream_id)
   then (
@@ -471,10 +477,8 @@ let process_first_headers_block t frame_header stream headers_block =
       2 * headers_block_length
   in
   let partial_headers =
-    { Stream.parse_state =
-        AB.parse
-          ~initial_buffer_size
-          (Hpack.Decoder.decode_headers t.hpack_decoder)
+      { Stream.parse_state =
+          AB.parse ~initial_buffer_size (decode_headers t)
     ; end_stream = Flags.test_end_stream flags
     }
   in
@@ -511,7 +515,7 @@ let process_trailer_headers t stream active_stream frame_header headers_block =
   else
     let partial_headers =
       { Stream.parse_state =
-          AB.parse (Hpack.Decoder.decode_headers t.hpack_decoder)
+          AB.parse (decode_headers t)
           (* obviously true at this point. *)
       ; end_stream
       }
diff --git a/lib/settings.ml b/lib/settings.ml
index 070d697..0904520 100644
--- a/lib/settings.ml
+++ b/lib/settings.ml
@@ -183,6 +183,11 @@ let settings_for_the_connection ?(include_enable_push = true) settings =
     then EnablePush (if settings.enable_push then 1 else 0) :: settings_list
     else settings_list
   in
+  let settings_list =
+    match settings.max_header_list_size with
+    | Some size -> MaxHeaderListSize size :: settings_list
+    | None -> settings_list
+  in
   settings_list

 let parse_settings_payload num_settings =
