# eta-ai

eta-ai is the core AI vocabulary package for Eta.

It provides provider records, messages, responses, raw JSON tool schemas, an
eta-http-backed SSE pull parser, GenAI telemetry wrappers, and redacted API-key
handling. Provider packages own provider-specific HTTP encoding and decoding.
Applications own state.

Start with ../../docs/tutorial-eta-ai.md.
